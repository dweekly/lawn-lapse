{
  "name": "lawn-lapse",
  "version": "0.2.1",
  "description": "Capture daily snapshots from UniFi Protect camera and create time-lapse videos",
  "type": "module",
  "bin": {
    "lawn-lapse": "lawn-lapse.js"
  },
  "scripts": {
    "capture": "node capture-and-timelapse.js",
    "setup:cron": "sh scripts/setup-cron.sh",
    "format": "prettier --write .",
    "lint": "eslint . --fix",
    "test": "node --test && node --check lawn-lapse.js && node --check capture-and-timelapse.js && echo '✓ Tests passed'",
    "postinstall": "sh scripts/setup-hooks.sh 2>/dev/null || true"
  },
  "dependencies": {
    "@inquirer/prompts": "^7.2.0",
    "dotenv": "^17.2.2",
    "node-fetch": "^3.3.2",
    "unifi-protect": "^4.27.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
